Replace embed-based query in `fetchAdminBnpl` (`src/lib/admin-api.ts`) with separate lookups for `profiles`, `pets`, and `vet_tickets`, then merge in JS. Same pattern as the vets fix. No DB / RLS changes.