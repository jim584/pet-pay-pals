
## Plan: Enlarge Homepage Logo

The logo is currently 60×84px. I'll increase it to **100×140px** (same aspect ratio) and add a bit more vertical padding so it looks prominent.

### Changes

**`src/pages/HomePage.tsx` (line 53)**
- Change logo dimensions from `{ width: 60, height: 84 }` to `{ width: 100, height: 140 }`
- Update header padding from `py-1` to `py-2` for balanced spacing
